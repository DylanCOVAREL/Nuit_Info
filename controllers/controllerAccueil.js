
const modele = require('../models/modelAccueil.js');

const sexinfo_accueil = (req, res) => {
    modele.find()
}

const sexinfo_quizz = (req, res) => {
    modele.find()
}

module.exports = {
    sexinfo_accueil,
    sexinfo_quizz
}